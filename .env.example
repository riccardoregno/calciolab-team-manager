VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Prototype only: client-side Vite variables are exposed in the browser.
# For production, move OpenAI calls to Supabase Edge Functions or another backend.
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-5-mini

# Local development only: unlock every Premium/Club feature while building.
VITE_UNLOCK_PREMIUM=false
